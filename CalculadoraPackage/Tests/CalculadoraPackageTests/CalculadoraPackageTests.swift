import XCTest
@testable import CalculadoraPackage

final class CalculadoraPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CalculadoraPackage().text, "Hello, World!")
    }
}
